# Copy this file to .env and adjust values as needed.
# Currently no secrets are required — all inference is local.

# Override Ollama URL if running on a different host or port
# OLLAMA_BASE_URL=http://localhost:11434

# Log level: DEBUG, INFO, WARNING, ERROR
# LOG_LEVEL=INFO
